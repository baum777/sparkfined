# URL der zu prüfenden Deployment-Instanz (Preview oder Prod)
VERIFY_BASE_URL=https://your-preview-or-prod.vercel.app

# Optional: Devnet Wallet für /api/access-status Test
VERIFY_DEV_WALLET=YOUR_DEVNET_PUBLIC_KEY

# Optional: Token Mint (nur Info, vom Script nicht genutzt)
ACCESS_TOKEN_MINT=REPLACE_WITH_DEVNET_SPL_MINT
